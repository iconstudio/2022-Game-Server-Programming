#ifndef PCH_H
#define PCH_H

#include <WinSock2.h>

#include <iostream>
#include <vector>
#include <map>
#include <algorithm>
#include <chrono>

using std::cout;
using std::cin;
using std::cerr;
using std::vector;
using std::map;
using std::begin;
using std::end;
using std::cbegin;
using std::cend;
using std::chrono::steady_clock;
using std::chrono::system_clock;

#endif //PCH_H
